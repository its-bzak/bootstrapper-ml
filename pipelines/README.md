# Pipelines

This directory contains repeatable ML workflows.

Examples:
- preprocessing pipelines
- training workflows
- batch inference jobs

Not every ML service requires pipelines.

For real-time inference services, model execution may live entirely inside the application layer.