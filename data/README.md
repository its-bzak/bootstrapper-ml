# Data

This directory is intended for projects that manage datasets locally.

Note that not every ML service requires local data storage.

Datasets may instead live in:
- cloud storage
- databases
- data warehouses
- external platforms
for production systems

***Do not commit large datasets***